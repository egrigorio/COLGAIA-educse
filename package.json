{
  "devDependencies": {
    "daisyui": "^4.8.0",
    "tailwindcss": "^3.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "",
    "dev": "npx tailwindcss -i input.css -o public/styles.css --watch"
  }
}
